<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @fact.title %>
</p>

<p>
  <b>Content:</b>
  <%= @fact.content %>
</p>

<p>
  <b>User:</b>
  <%= @fact.user_id %>
</p>


<%= raw [can?(:edit, @fact) ? link_to('Edit', edit_fact_path(@fact)) : nil, link_to('Back', facts_path)].compact.join " | " %>
